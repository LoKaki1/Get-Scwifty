class GameGrid extends GridBase {

    constructor (length) {
        super(length)
        this.emptyCell = null
    }

    MixGrid() {
        this.cells = []
        for (let i = 0; i < length; i++) {
            this.cells.push
        }
    }

    SwapWithEmptyCell(cell) {

    }
}