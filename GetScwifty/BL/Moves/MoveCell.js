class MoveCell {
    constructor (possibleMoves, GameGrid) {
        this.possibleMoves = possibleMoves
        this.GameGrid = GameGrid
    }
    Swap(cell) {
        // bool returns true if succeeds else false
    }
}